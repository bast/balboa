language: python

sudo: false

matrix:
  include:
    - os: linux
      compiler: gcc
      addons: &gcc49
        apt:
          sources: ['ubuntu-toolchain-r-test']
          packages: ['g++', 'gcc']
      env:
        - CXX='g++'
        - CC='gcc'

install:
  - pip install -r requirements.txt

script:
  - ./setup
  - cd build
  - make
  - cd ..
  - pep8 test/test.py
  - PROJECT_BUILD_DIR=$PWD/build PYTHONPATH=$PWD/api py.test -s -vv test/test.py
  - py.test -s -vv src/generate.py

notifications:
  email: false
